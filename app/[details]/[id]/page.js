import React from "react";

function page({ params }) {
  console.log(params);
  //   if (params.discover !== "movie" && params.discover !== "tv") {
  //     notFound();
  //     return;
  //   }
  return <div>page</div>;
}

export default page;
